# EchoTale Frontend

Next.js frontend for EchoTale audio storytelling app.

## Run locally

```bash
npm install
npm run dev
```

Create `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
```

Open `http://localhost:3000`.
